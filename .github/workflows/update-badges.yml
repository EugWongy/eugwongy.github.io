name: Update badges

on: workflow_dispatch
#  schedule:
    # Runs at 0am UTC every day
#    - cron: "0 0 * * *"
jobs:
  update-professional:
    name: Update professional with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - professional
        uses: pemtajo/badge-readme@main
        with:       
          CREDLY_USER: eugene-wong.71f6a6cd
